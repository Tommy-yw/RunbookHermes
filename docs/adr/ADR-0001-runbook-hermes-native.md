# ADR-0001: RunbookHermes is Hermes-native

Decision: keep the official Hermes-agent source tree as the project root and implement RunbookHermes as a vertical incident-response profile, plugin, memory provider, context engine and runbook skills.

Old Week7 RunbookHermes code is reference material only. New functionality must not import legacy orchestrator code.
